# Ficheiro: admin/config.yml (VERSÃO CORRIGIDA)

backend:
  name: git-gateway
  branch: main

media_folder: "imagens/uploads"
public_folder: "/imagens/uploads"

collections:
  - name: "artistas"  # O nome da coleção
    label: "Artistas"  # O nome que aparece no painel
    file: "_data/artistas.json"  # IMPORTANTE: Salva TUDO neste ficheiro único
    label_singular: "Artista"
    description: "Adicione, edite e remova artistas da galeria aqui"
    widget: "list"  # A coleção inteira é uma lista de artistas
    fields:  # Os campos para CADA artista na lista
      - { label: "ID (Nome único, sem espaços, ex: LIGHT)", name: "id", widget: "string" }
      - { label: "Nome de Exibição", name: "nome", widget: "string" }
      - { label: "Logo do Artista", name: "logo", widget: "image" }
      - { label: "Link do Instagram (URL completa)", name: "instagramLink", widget: "string" }
      - { label: "Usuário do Instagram (ex: @LIGHT_452)", name: "instagramHandle", widget: "string" }
      - label: "Galeria de Imagens"
        name: "imagens"
        widget: "list"
        summary: "{{fields.imagem}}"
        field: { label: "Imagem", name: "imagem", widget: "image" }